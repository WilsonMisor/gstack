# General / Non-WordPress Overlay

Apply the 75 controls and PREOS through the real stack: frontend, API, backend, database, storage, authentication, authorization/tenant boundaries, queues, caches, CDN, hosting/cloud, CI/CD, observability and recovery. Deny by default where authorization is uncertain. Frontend checks are usability controls, not security boundaries. Use native row/tenant isolation mechanisms where supported and appropriate.

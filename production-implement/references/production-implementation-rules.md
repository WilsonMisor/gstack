# Production Implementation Rules

Prompt = current intent. Project Contract = application truth. PREOS = production intelligence. gstack = workflow. Domain skills = specialist implementation knowledge. Evidence = proof. Conversation memory is never authoritative state. Implement the smallest coherent production-complete unit inside the approved lake; report adjacent work instead of silently expanding scope.

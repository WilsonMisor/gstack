# Project Contract Rules

The Project Contract is a versioned execution contract, not a replacement for source documents. Every material requirement keeps a stable ID, original wording/source location, authority, status and traceability. Authoritative source hash changes make affected contract sections STALE. Human approval is version-scoped and must be recorded explicitly.
